
# all props here just override the same ones in application.properties, 
# for @SpringBootTest annotated tests which also have the following annotation: 
# @TestPropertySource(locations = "classpath:/application-SpringBootTest.properties") 

# database type
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.datasource.driver-class-name=org.h2.Driver

# DDL actions policy - TODO : needed ??? what setting ???
#spring.jpa.hibernate.ddl-auto=update

# DB url
spring.datasource.url = jdbc:h2:mem:test
